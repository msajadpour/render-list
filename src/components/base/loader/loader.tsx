// styles
import Styles from "./loader.module.scss";

const Loader = () => {
    return (
        <div className={Styles.parent}>
            <div></div>
        </div>
    );
};

export default Loader;
